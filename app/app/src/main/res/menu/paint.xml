<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_set_background"
        android:icon="@drawable/ic_photo_library_black"
        android:title="@string/set_background_item"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_clear"
        android:icon="@drawable/ic_clear_black"
        android:title="@string/clear_item"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_share_black"
        android:title="@string/paint_share_item"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/action_save"
        android:icon="@drawable/ic_save_black_24dp"
        android:title="@string/save_item"
        app:showAsAction="ifRoom"/>
</menu>